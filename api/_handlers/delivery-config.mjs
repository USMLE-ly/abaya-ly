// Delivery policy — server-side mirror of src/lib/delivery.ts.
// Free delivery inside Benghazi; a flat fee for every other city.
export const DELIVERY = {
  freeCity: "بنغازي",
  fee: 15, // د.ل — applied to all cities except the free city
};
