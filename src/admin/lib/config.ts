// Admin dashboard path configuration.
// Change this value to move the admin dashboard to a different path.
// This prevents attackers from discovering the admin login page.
export const ADMIN_PATH = import.meta.env.VITE_ADMIN_PATH || "dashboard-nadine-admin";
